- name: Installer kibana
  ansible.builtin.apt:
    name: kibana
    state: present

- name: Create cert dir
  ansible.builtin.file:
    path: /etc/kibana/certs
    state: directory
    owner: kibana
    group: kibana
    mode: "0700"