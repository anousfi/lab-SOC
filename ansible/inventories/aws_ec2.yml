plugin: amazon.aws.aws_ec2

regions:
  - eu-west-3

filters:
  tag:Environment: lab

hostnames:
  - instance-id

compose:
  ansible_connection: amazon.aws.aws_ssm

keyed_groups:
  - key: tags.Role
    prefix: role