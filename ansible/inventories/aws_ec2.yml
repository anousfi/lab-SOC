plugin: amazon.aws.aws_ec2

regions:
  - eu-west-3

filters:
  tag:Environment: lab

hostnames:
  - tag:Name
  - dns-name
  - private-ip-address

compose:
  ansible_host: private_ip_address
  ansible_user: "'ubuntu'"

keyed_groups:
  - key: tags.Role
    prefix: role